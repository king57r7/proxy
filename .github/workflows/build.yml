name: Verify and Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  api-smoke:
    name: Railway API smoke test
    runs-on: ubuntu-latest
    steps:
      - name: Verify liveness
        run: curl --fail --silent --show-error https://proxy-production-58ff.up.railway.app/livez
      - name: Verify readiness
        run: curl --fail --silent --show-error https://proxy-production-58ff.up.railway.app/readyz
      - name: Verify products contract
        run: curl --fail --silent --show-error https://proxy-production-58ff.up.railway.app/api/v1/products

  backend:
    name: Backend quality gates
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run type-check
      - run: npm test
      - run: npm run build
      - run: npm audit --omit=dev --audit-level=high

  android:
    name: Android lint, tests, and debug APK
    needs: [api-smoke, backend]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
        with:
          accept-android-sdk-licenses: true
      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"
      - name: Configure API endpoint
        run: echo "API_BASE_URL=${{ vars.API_BASE_URL || 'https://proxy-production-58ff.up.railway.app/api/v1' }}" >> gradle.properties
      - run: chmod +x gradlew
      - run: ./gradlew lintDebug testDebugUnitTest assembleDebug --no-daemon
      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: proxy-platform-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 14
